var Assert = (function() {
    function Assert() {}
    Assert.prototype.isDefined = function(expr, msg) {
        if(expr == null || expr == '') {
            throw "Not defined : " + msg;
        }
    }
    return Assert;
})();
module.exports = Assert;